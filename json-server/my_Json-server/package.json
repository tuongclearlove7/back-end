{
  "name": "json-server",
  "version": "1.0.0",
  "description": "this is my json server",
  "main": "index.js",
  "scripts": {
    "start" : "json-server --watch data.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "clearlove7",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
