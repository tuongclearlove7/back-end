# Push project to the github repository

```js
bước 1 : chuột phải bên ngoài màn hình rồi chọn git base here  => gõ git clone  
sau đó copy dòng này trên repo https://github.com/tuongclearlove7/C.git 
=> paste vào sau git clone enter 
rồi  clone copy paste source code vào file vừa clone => gõ git add .
=>  rồi vô repo copy dòng git commit -m "first commit"
=> gõ git push

```